#pragma once
#ifndef RECTANGLE_HPP
#define RECTANGLE_HPP

struct Rectangle {
	int width;
	int height;

	int Area(){ return width * height; };

} Box;	//Optional Field
		//Can use this name to declare objects of the structure type

#endif